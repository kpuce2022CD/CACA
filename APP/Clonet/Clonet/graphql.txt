
# 로그인
query login {
  login($user_id: user_id, $user_pw: user_pw) {
    user_id
    user_pw
    user_name
    user_email
    profilePic
    about
    contact
  }
}

# 회원가입
mutation signup($user_id: user_id, &user_pw: user_pw, $user_name: user_name, $user_email: user_email, $profilePic: profilePic){
    signup($user_id: user_id, &user_pw: user_pw, $user_name: user_name, $user_email: user_email, $profilePic: profilePic){
    user_id
    user_pw
    user_name
    user_email
    profilePic
  }
}

# 아이디 찾기
query find_id($user_email: user_email){
    find_id($user_email: user_email){
        user_id
        user_pw
        user_name
        user_email
        profilePic
        about
        contact
  }
}

# 비밀번호 찾기
query find_pw($user_id: user_id){
    find_pw($user_id: user_id){
        user_id
        user_pw
        user_name
        user_email
        profilePic
        about
        contact
  }
}

# 리포 목록
query repo_list($user_id){
    repo_list($user_id) {
        user_id
        repo_name
  }
}

# 리포 생성
mutation create_repo($repo_name: repo_name, $repo_ec2_ip: repo_ec2_ip, $user_id: user_id){
    insert_repo($repo_name: repo_name, $repo_ec2_ip: repo_ec2_ip){
        repo_name
        repo_ec2_ip
  }
  insert_mapping($user_id: user_id, $repo_name: repo_name){
    user_id
    repo_name
  }
}

# 사용자 추가
query checkUser($user_id: user_id){
    checkUser($user_id: user_id){
        user_id
        user_pw
        user_name
        user_email
        profilePic
    }
}

mutation insertUser($user_id: user_id, $repo_name: repo_name){
    insertUser($user_id: user_id, $repo_name: repo_name){
        user_id
        repo_name
  }
}

# 사용자 프로필 select
query selectUserProfilePic($user_id: user_id){
    selectUserProfilePic($user_id: user_id){
        user_id
        user_pw
        user_name
        user_email
        profilePic
        about
        contact
    }
}

# 사용자 프로필 insert
mutation insertUserProfilePic($user_id: user_id, $profilePic: profilePic){
    insertUserProfilePic($user_id: user_id, $profilePic: profilePic){
        user_id
        user_pw
        user_name
        user_email
        profilePic
        about
        contact
  }
}

# 그룹 사용자
query selectGroupUser($repo_name: repo_name){
    selectGroupUser($repo_name: repo_name){
        user_id
        repo_name
    }
}
 
# log_query
query selectLog {
  log {
    user_id
    commit_id
    commit_msg
    date
  }
}

##

query Query {

launches{
user_id
user_pw
user_name
user_email
profilePic
about
contact
},

repository{
repo_name
repo_ec2_ip
},

mapping_repo_user{
user_id
repo_name
},

log {
user_id
commit_id
commit_msg
date
}
}
}
